interface messageProps {
  message: string;
  text: string;
  url: string;
  img: string;
  reply: string;
  preview: string;
  incoming: boolean;
  outgoing: boolean;
  menu: boolean;
}

export default messageProps;
